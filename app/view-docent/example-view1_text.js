'use strict';

describe('myApp.view-docent module', function() {

  beforeEach(module('myApp.view-docent'));

  describe('view-docent controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});